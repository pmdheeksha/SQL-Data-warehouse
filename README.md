# SQL-Data-warehouse
Built a Sales Data Warehouse using Medallion Architecture (Bronze, Silver, Gold) in SQL Server. Implemented ETL processes to transform raw data into structured fact and dimension tables for reporting and analytics.
